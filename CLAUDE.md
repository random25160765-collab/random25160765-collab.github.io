# CLAUDE.md

这份文件为 Claude Code（claude.ai/code）在此仓库中工作时提供指引。

## 概述

基于 GitHub Pages 的静态个人网站，域名为 `random25160765-collab.github.io`。推送到 `main` 分支即自动部署。

## 仓库结构

- `index.html` — 主页（HTML + 内联 CSS）
- 所有内容均为纯静态文件（HTML、CSS、JS），无构建步骤、无框架、无依赖。

## 部署

推送到 `main` 分支即可，GitHub Pages 会自动发布该分支根目录下的内容。
commit 的作者不要添加 claude。
